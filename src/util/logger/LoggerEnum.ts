/* eslint-disable no-unused-vars */
export enum LogLevel {
  TRACE,
  DEBUG,
  INFO,
  CONFIG,
  WARN,
  ERROR,
  FATAL,
  SEVERE,
  AUDIT,
  STATS,
}

export enum LogDestination {
  CONSOLE,
  FILE,
}
